# Tableau de Suivi des Résidents

Application pour le suivi des résidents.

## Build

Après chaque commit, veuillez lancer le script `build.js` pour mettre à jour le build de l'application.

```bash
node build.js
```
